#!/bin/sh

set -eaux

#
 datadir=/export-6/cacsrv1/wd52pp/LF_prd
 lcdir=/export-6/sgi9/wd52pp/project/LF_prd
 tmp=/export-6/sgi9/wd52pp/tmp
#
 cp skill_2D.f $tmp
 cd $tmp
#
cat > parm.h << eof
       parameter(imax=128,jmax=64)
       parameter(ltime=22,ifld=3)  ! ifld < 3 for hgt500 and prate; =3 for temp
       parameter(iskip=29)
eof
#
 f77  skill_2D.f
#
#
 \rm fort.*
   ln -s $datadir/   fort.10
   ln -s $datadir/   fort.20
 
   ln -s $datadir/  fort.50
#
#
 a.out 
#
ja -s
